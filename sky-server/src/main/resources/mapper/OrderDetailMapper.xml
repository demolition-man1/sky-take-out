<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail (order_id, dish_id, setmeal_id, name, image, flavor, number, amount)
        values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.name}, #{item.image},
             #{item.flavor}, #{item.number}, #{item.amount})
        </foreach>
    </insert>

    <select id="getByOrderIds" resultType="com.sky.entity.OrderDetail">
        select * from order_detail
        <where>
            <if test="orderIds != null and orderIds.size() > 0">
                order_id in
                <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
                    #{orderId}
                </foreach>
            </if>
        </where>
    </select>

</mapper>
